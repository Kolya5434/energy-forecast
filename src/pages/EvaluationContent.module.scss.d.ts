declare const styles: {
  readonly evaluationContent: string;
  readonly paper: string;
  readonly controls: string;
  readonly modelSelector: string;
  readonly chipContainer: string;
};

export default styles;
